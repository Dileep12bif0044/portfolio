import styled from "styled-components";

export const AboutWrapper = styled.div`
    .main {
        margin: auto;
        width: 750px;
        max-width: calc(100vw - 50px);
    },

    .main-image {
        width:auto;
        height:350px;
    }
`